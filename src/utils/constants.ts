export const HOME_PAGE_QUERIES = {
    autoLoggedOut: {
        login: 'expired'
    }
}

export const HOME_PAGE_QUERY_KEY_VALUE = {
    autoLoggedOut: {
        key: 'login',
        value: 'expired'
    }
}